services:
  web:
    container_name: web
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
    - .:/app
    #"react-app"の部分がプロジェクト名になる
    command: sh -c "cd bottlediver && yarn start" 
    ports:
      - 3000:3000
    stdin_open: true #標準入力をコンテナと紐付ける

